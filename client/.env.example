# Example environment variables for production build
# Copy this to `.env.production` locally (do NOT commit secrets).
# On Vercel set an Environment Variable named `REACT_APP_API_BASE` with your server URL.

# Example: REACT_APP_API_BASE="https://your-server.example.com"
REACT_APP_API_BASE="http://localhost:5001"
